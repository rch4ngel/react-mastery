import React from 'react'

// ES6 Function syntax.
const person = () => {
  return (
      <div className={person}>
        <h2>Person {Math.floor(Math.random() * 30)}</h2>
      </div>
  )
};

export default person;